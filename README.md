# 🌐 Генератор файла geosite.dat

Утилита для автоматической генерации файлов geosite.dat, используемых для маршрутизации трафика в прокси-серверах (V2Ray, Xray, Clash).

### 🚀 Быстрый запуск

Автоматический запуск (рекомендуется):

```bash
sudo bash -c "$(wget -qO- https://raw.githubusercontent.com/makdren/geosite-generator/main/dlcgen.sh)"
```

Или через curl:

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/makdren/geosite-generator/main/dlcgen.sh)"
```

Ручной запуск:

```bash
# Скачать скрипт
wget https://raw.githubusercontent.com/makdren/geosite-generator/main/dlcgen.sh

# Сделать исполняемым
chmod +x dlcgen.sh

# Запустить
sudo ./dlcgen.sh
```

📋 Как это работает

1. Создайте форк репозитория v2fly/domain-list-community
2. Добавьте свои домены в папку data/:
   · Создайте файл с именем категории (например, vk, ru-services)
   · Добавьте домены по одному на строку
   · Сохраните и опубликуйте изменения
3. Запустите скрипт и укажите ссылку на свой форк

📁 Результат

После выполнения файлы будут созданы в:

```
/root/dlcgen/
├── geosite.dat     # Основной файл для использования
├── dlc.dat         # Альтернативный формат
└── [исходные файлы]
```

⚠️ Важное предупреждение

Не удаляйте существующие файлы из папки data/!
При удалении файлов скрипт перестает работать. Можно только добавлять новые файлы или редактировать существующие.

🐛 Устранение неполадок

Ошибка клонирования:

· Проверьте корректность URL репозитория
· Убедитесь, что репозиторий публичный

Docker не установлен:

· Установите вручную: sudo apt install docker.io

Ошибка генерации:

· Убедитесь, что в папке data/ есть файлы
· Проверьте формат файлов (один домен на строку)
